name: Bot Tests

on:
  pull_request:
    paths:
      - 'packages/bot/**'
  push:
    branches: [main]
    paths:
      - 'packages/bot/**'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Lint bot code
        run: pnpm --filter @arbimind/bot lint

      - name: Run bot tests
        run: pnpm --filter @arbimind/bot test:coverage
