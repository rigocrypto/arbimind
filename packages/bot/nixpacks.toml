[phases.setup]
cmds = ["npm i -g pnpm", "corepack disable"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]
[phases.build]
cmds = ["pnpm --filter @arbimind/bot build"]

[start]
cmd = "pnpm --filter @arbimind/bot start"
