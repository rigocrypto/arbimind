[phases.setup]
cmds = ["corepack disable"]

[phases.install]
cmds = ["npx pnpm@latest install --frozen-lockfile"]
[phases.build]
cmds = ["npx pnpm@latest --filter @arbimind/bot build"]

[start]
cmd = "npx pnpm@latest --filter @arbimind/bot start"
