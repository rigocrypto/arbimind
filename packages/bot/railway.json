{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --dir packages/bot build"
  },
  "deploy": {
    "startCommand": "pnpm --dir packages/bot build && pnpm --dir packages/bot start"
  }
}
